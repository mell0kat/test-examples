/*
  Empty mock to avoid CSS parsing errors.

  The moduleNameMapper property in package.json
  will replace all .css module imports with this file
  so that jest doesn't try to run them as javascript
*/

module.exports = {}
